# CloudWatch Logs Insights Query
# Description: Analyze business metrics for the last 24 hours
# Log Group: /aws/elasticbeanstalk/subscriptions-contracts-eb/business-metrics
# Time Range: Last 24 hours

fields @timestamp, metric, value, metadata
| stats count(*) as event_count by metric
| sort event_count desc
