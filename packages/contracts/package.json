{
  "name": "@rt/contracts",
  "version": "1.0.0",
  "description": "Shared contracts, types and schemas for RT Technologie",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo",
    "lint": "tsc --noEmit"
  },
  "keywords": ["contracts", "types", "schemas"],
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
